import { Route, Routes } from '@angular/router';
import { HomeScreenComponent } from './Components/home-screen/home-screen.component';

export const routes: Route[] = [{ path: '', component: HomeScreenComponent }];
